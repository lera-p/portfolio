/* -- Left inner menu animation (begin) -- */jQuery(function(){	jQuery('.l-side-menu li a').on({		click : function(e){			var $parent = jQuery(this).parents('.l-side-menu li:eq(0)');			if ($parent.find('> ul').length) {				if ($parent.find('> ul:hidden').length) {					$parent.addClass('additional-menu-item_current');				} else {					$parent.removeClass('additional-menu-item_current');				}				$parent.find('> ul').slideToggle(200);				e.preventDefault();			}		}	})});/* -- Left inner menu animation (end) -- *//* -- Star Toggle -- */jQuery('.star-icon:not(.disable)').on('click', function(){	 	jQuery(this).toggleClass('active').parents('.job-item-link').toggleClass('active');});/* -- Profile Navigation -- */jQuery('.b-profile__link-wrp').on('click', function(){	 	jQuery(this).toggleClass('active').find('.profile-navigation').fadeToggle(10);});/* -- Dropdown Menu -- */$('.dropdown-menu a').on('click', function(e){	var linktext = $(this).text()	$(this).parents('ul').find('li').removeClass('disabled')	$(this).parent().addClass('disabled')	$(this).parents('ul').prev().contents().filter(function(){ 	  return this.nodeType == 3; 	})[0].nodeValue = linktext	e.preventDefault();})// init bootpag page navigation$('.pagenav').bootpag({   total: 20,   page: 1,   maxVisible: 7,   next: 'Newer',   prev: 'Older' }).on('page', function(event, num){    $(".content2").html("Page " + num); // or some ajax content loading...});// Textarea validation$('.textarea-val').on('blur change keyup', function(){	if (!$.trim($(this).val())) {	   $(this).next().prop('disabled', true);	   $(this).addClass('dis');	} else {		$(this).next().removeAttr("disabled")		$(this).removeClass('dis');	}});// Collapse section$('.collapse-nav').on('click', function(e){	$(this).toggleClass('collapsed').parents('.navbar-collapse').next().slideToggle(250);	e.preventDefault();});