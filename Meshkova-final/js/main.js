//sliders for the calculator$(function() {      $( "#slider" ).slider({     range: "min",     value: 200,     min: 10,     max: 300,     step: 5,     animate: true,     slide: function( event, ui ) {       $( "#amount" ).val( ui.value );     }   });   $( "#amount" ).val( $( "#slider" ).slider( "value" ) );      $( "#slider-range-time" ).slider({     range: "min",     value: 20,     min: 1,     max: 30,     step: 1,     animate: true,     slide: function( event, ui ) {       $( "#times" ).val( ui.value );     }   });   $( "#times" ).val( $( "#slider-range-time" ).slider( "value" ) ); });
// SELECT CuSTOM$(function(){	$('.select').selectric();});//Toggle color $(function(){	$('.color-item').on('click', function(){		$(this).addClass('active').siblings().removeClass('active');	})});// COLORBOX$('.popup-btn').colorbox({inline:true, width:"50%"});//SLIDER(function($) {    // This is the connector function.    // It connects one item from the navigation carousel to one item from the    // stage carousel.    // The default behaviour is, to connect items with the same index from both    // carousels. This might _not_ work with circular carousels!    var connector = function(itemNavigation, carouselStage) {        return carouselStage.jcarousel('items').eq(itemNavigation.index());    };    $(function() {        // Setup the carousels. Adjust the options for both carousels here.        var carouselStage      = $('.carousel-stage').jcarousel();        var carouselNavigation = $('.carousel-navigation').jcarousel();        // We loop through the items of the navigation carousel and set it up        // as a control for an item from the stage carousel.        carouselNavigation.jcarousel('items').each(function() {            var item = $(this);            // This is where we actually connect to items.            var target = connector(item, carouselStage);            item                .on('jcarouselcontrol:active', function() {                    carouselNavigation.jcarousel('scrollIntoView', this);                    item.addClass('active');                })                .on('jcarouselcontrol:inactive', function() {                    item.removeClass('active');                })                .jcarouselControl({                    target: target,                    carousel: carouselStage                });        });        // Setup controls for the stage carousel        $('.prev-stage')            .on('jcarouselcontrol:inactive', function() {                $(this).addClass('inactive');            })            .on('jcarouselcontrol:active', function() {                $(this).removeClass('inactive');            })            .jcarouselControl({                target: '-=1'            });        $('.next-stage')            .on('jcarouselcontrol:inactive', function() {                $(this).addClass('inactive');            })            .on('jcarouselcontrol:active', function() {                $(this).removeClass('inactive');            })            .jcarouselControl({                target: '+=1'            });    });})(jQuery);//SLIDER-BOTTOM(function($) {    $(function() {        $('.jcarousel').jcarousel();        $('.jcarousel-control-prev')            .on('jcarouselcontrol:active', function() {                $(this).removeClass('inactive');            })            .on('jcarouselcontrol:inactive', function() {                $(this).addClass('inactive');            })            .jcarouselControl({                target: '-=1'            });        $('.jcarousel-control-next')            .on('jcarouselcontrol:active', function() {                $(this).removeClass('inactive');            })            .on('jcarouselcontrol:inactive', function() {                $(this).addClass('inactive');            })            .jcarouselControl({                target: '+=1'            });        $('.jcarousel-pagination')            .on('jcarouselpagination:active', 'a', function() {                $(this).addClass('active');            })            .on('jcarouselpagination:inactive', 'a', function() {                $(this).removeClass('active');            })            .jcarouselPagination();    });})(jQuery);//Tabs for sliders(function($) {  $(function() {      $('.sliders-nav').on('click', 'span:not(.current)', function() {      $(this).addClass('current').siblings().removeClass('current')        .parents('.bottom-sliders').find('.slider-box').eq($(this).index()).fadeIn(150).siblings('.slider-box').hide();   })   });  })(jQuery);//input phone$(function(){		$('.form-item-type2 input').keyup(function(e){			if($(this).val().length==$(this).attr('maxlength'))				$(this).next(':input').focus()		})	})	 // Form  $(function() {        $('#delivery').on('click', '.radio-fld:not(.active)', function() {       $(this).addClass('active').siblings().removeClass('active')         .parents('.fieldset').find('.address-vivoz').toggleClass('active').prev().toggleClass('visible');   })    $('.delivery-fld').on('click', 'input', function() {      if ($('#a21').is(':checked') || $('#a31').is(':checked')) {    	$(this).parents('.main-count-form').find('.gray-btn').addClass('active');    	$('.text-fld:first input').removeClass('ui-state-error');    	$('.address-fields').find("[required='required']").each( function(idx, element) {    	       $(element).removeAttr('required');    	   });    	   $('.selectric').removeClass('error');      	} else  {    		$(this).parents('.main-count-form').find('.gray-btn').removeClass('active');    		$('.address-fields').find(".text-fld:first input, select").each( function(idx, element) {    		       $(element).attr("required", "true");    		   });    	}    if ($('.address-vivoz').hasClass('active')) {    	$(this).parents('fieldset').addClass('empty');    }	else {    	$(this).parents('fieldset').removeClass('empty');    }     })          $('.radio-group.visibility').on('click', 'input', function() {      if ($('.radio-group.visibility input').is(':checked')) {     	$(this).parents('.main-count-form').find('.gray-btn').addClass('active');       }      })            $('.final-form-info').on('click', '.gray-btn.active', function(){      	$('.data-form').fadeIn(300);      	$('.data-form__box:hidden').find("[required='required']").each( function(idx, element) {      	       $(element).removeAttr('required');      	   });      	   $(this).parents('.main-count-form').find('fieldset:last').removeClass('error');        })            $('.final-form-info').on('click', '.gray-btn:not(.active)', function() {          if ($('input[name="place3"]:checked').length > 0) {        	$(this).parents('.main-count-form').find('fieldset:last').removeClass('error');        } else {        	$(this).parents('.main-count-form').find('fieldset:last').addClass('error');        }      })            $('.delivery-fld').on('click', '#a41', function() {         	  $('.text-fld:first input').addClass('ui-state-error');      	   $('.selectric').addClass('error');       })              $('.address-fields').on('change keyup', '.text-fld:first input, select', function(){       		if ($('.select2').val().length && $('.text-fld:first input').val().length && $('.select').val().length) {       			$(this).parents('.main-count-form').find('.gray-btn').addClass('active');       		} else {       			$(this).parents('.main-count-form').find('.gray-btn').removeClass('active');       		}        })            $('.toggle-form').on('click', 'label', function() {          if ($(this).hasClass('fiz-l')) {        	$(this).parents('.data-form').find('.data-form__box.hide').fadeIn(150).siblings('.data-form__box').hide()        	.parents('.data-form').find('.fieldset__toggle').hide();        } else {        	$(this).parents('.data-form').find('.data-form__box.hide').hide().siblings('.data-form__box').fadeIn(150)        	.parents('.data-form').find('.fieldset__toggle').show();        }        $('.data-form__box:hidden').find("[required='required']").each( function(idx, element) {               $(element).removeAttr('required');           });        $('.data-form__box:visible').find("input").each( function(idx, element) {               $(element).attr("required", "true");           });        })     $('#formTemplate').on('click', function() {      	$('.select').parent().next().addClass('error');  	if ($('.select').hasClass('ui-state-valid')) {  		$('.select.ui-state-valid').parent().next().removeClass('error');  	}	else if ($('#a21').is(':checked') || $('#a31').is(':checked'))  {  		$('.selectric').removeClass('error');  	}   	else {  		$('.selectric').addClass('error');  	} });   $('.selectricItems li').click(function() {   $(this).parents('.selectricWrapper').find('.selectric').removeClass('error'); });  });  $('#formTemplate').submit(function() {  if (!attributeSupported("required") || ($.browser.safari)) {   //If required attribute is not supported or browser is Safari (Safari thinks that it has this attribute, but it does not work), then check all fields that has required attribute   $("#formTemplate [required]").each(function(index) {    if (!$(this).val()) {     //If at least one required value is empty, then ask to fill all required fields.     alert("Please fill all required fields.");     return false;    }   });  }  return false; //This is a test form and I'm not going to submit it}); 