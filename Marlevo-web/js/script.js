/* -- Left inner menu animation (begin) -- */jQuery(function(){	jQuery('.l-side-menu li a').on({		click : function(e){			var $parent = jQuery(this).parents('.l-side-menu li:eq(0)');			if ($parent.find('> ul').length) {				if ($parent.find('> ul:hidden').length) {					$parent.addClass('additional-menu-item_current');				} else {					$parent.removeClass('additional-menu-item_current');				}				$parent.find('> ul').slideToggle(200);				e.preventDefault();			}		}	})});/* -- Left inner menu animation (end) -- *//* -- Messages notification (begin) -- */	jQuery('.message-icon').on('click', function(){	  jQuery(this).toggleClass('active').next().fadeToggle(70);	});/* -- Messages notification (end) -- *//* -- Profile info block  (begin) -- */	jQuery('.expand-action-btn').on('click', function(){	  if (jQuery(this).next().find('> .more-expand-item').length) {		jQuery(this).toggleClass('active').next().toggleClass('active').find('.more-expand-item').slideToggle(100);	  } 	  	});/* -- Profile info block (end) -- *//* -- Star Toggle -- */jQuery('.star-icon:not(.disable)').on('click', function(){	 	jQuery(this).toggleClass('active').parents('.job-item-link').toggleClass('active');});// init bootpag page navigation$('.pagenav').bootpag({   total: 20,   page: 1,   maxVisible: 7,   next: 'Newer',   prev: 'Older' }).on('page', function(event, num){    $(".content2").html("Page " + num); // or some ajax content loading...});// Min wage slider $( ".slider-range-search" ).slider({  range: "min",  value: 20,  min: 1,  max: 35,  step: 1,  animate: true,  slide: function( event, ui ) {    $(this).parent().parent().find( ".percent-inp" ).val( ui.value );    $( ".b-pcent", this  ).text( ui.value );  }});$( ".percent-inp" ).val( $( ".slider-range-search" ).slider( "value" ) );$( ".b-pcent" ).text( $( ".slider-range-search" ).slider( "value" ) );//Apply button$('.apply-btn').on('click', function(e){	if ($(this).hasClass('active')) {			$('.apply-btn').removeClass('active').text('Apply');		} else {			$('.apply-btn').addClass('active').text('Applied');		}	e.preventDefault();});//placeholder$('[placeholder]').focus(function() {    var input = $(this);    if (input.val() == input.attr('placeholder')) {        input.val('');        input.removeClass('placeholder');    }}).blur(function() {    var input = $(this);    if (input.val() == '' || input.val() == input.attr('placeholder')) {        input.addClass('placeholder');        input.val(input.attr('placeholder'));    }}).blur();$('[placeholder]').parents('form').submit(function() {    $(this).find('[placeholder]').each(function() {        var input = $(this);        if (input.val() == input.attr('placeholder')) {            input.val('');        }    })});